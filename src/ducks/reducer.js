import axios from 'axios'

const initalState = {
  username= '',
  userId= null,
  profilePicture= ''
}

export default function reducer(state = initialState, action) {
  switch (action.type) {

  }
}
